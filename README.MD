### Minimal working example of testing session variables stored in cookies on test

How to run:
* clone the repository
* create `venv` with Python 3.7.6 and activate
* `pip install -r requirements.txt`
